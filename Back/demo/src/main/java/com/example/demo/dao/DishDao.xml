<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.DishDao">

	<insert id="add" parameterType="map" useGeneratedKeys="true"
		keyProperty="dnum">
		insert into dish 
		set writer=#{writer}, title=#{title}, ingredient=#{ingredient}, recipe=#{recipe},
		url=#{url}, date = NOW(),
		t_time=#{t_time}, m_ingredient=#{m_ingredient}, cookery=#{cookery}, t_weather=#{t_weather}
	</insert>
	
	<insert id="addPicture" parameterType="map" useGeneratedKeys="true"
		keyProperty="pnum">
		insert into dish_picture 
		set pname=#{pname}, dnum= (select if(dnum=null, 1, max(dnum)) from dish)
	</insert>
	
	<insert id="editPicture" parameterType="map" useGeneratedKeys="true"
		keyProperty="pnum">
		insert into dish_picture 
		set pname=#{pname}, dnum=#{dnum}
	</insert>
	

	<select id="getOne" parameterType="Integer" resultType="map">
		SELECT
		d_l.*, 
		db.rcp_seq,
		db.rcp_nm,
		db.rcp_parts_dtls,
		db.rcp_pat2,
		db.rcp_way2,
		db.hash_tag,
		db.info_na,
		db.info_wgt,
		db.info_pro,
		db.info_fat,
		db.info_car,
		db.info_eng,
		db.manual_img01,
		db.manual_img02,
		db.manual_img03,
		db.manual_img04,
		db.manual_img05,
		db.manual_img06,
		db.manual_img07,
		db.manual_img08,
		db.manual_img09,
		db.manual_img10,
		db.manual_img11,
		db.manual_img12,
		db.manual_img13,
		db.manual_img14,
		db.manual_img15,
		db.manual_img16,
		db.manual_img17,
		db.manual_img18,
		db.manual_img19,
		db.manual_img20,
		db.manual01,
		db.manual02,
		db.manual03,
		db.manual04,
		db.manual05,
		db.manual06,
		db.manual07,
		db.manual08,
		db.manual09,
		db.manual10,
		db.manual11,
		db.manual12,
		db.manual13,
		db.manual14,
		db.manual15,
		db.manual16,
		db.manual17,
		db.manual18,
		db.manual19,
		db.manual20,
		db.att_file_no_main,
		db.att_file_no_mk  
		from 
		(SELECT 
		d.*, 
		l.lnum, 
		l.mnum, 
		l.date as like_date 
		from dish_like l right join dish d 
		on l.RCP_SEQ = d.RCP_SEQ) d_l 
		right join 
		dishDB db 
		on db.RCP_SEQ = d_l.RCP_SEQ
		where db.RCP_SEQ=#{RCP_SEQ}
	</select>
	 
	<select id="get" parameterType="Integer" resultType="map">
		SELECT
		d.writer,
		d.url,
		d.dish_like,
		d.ate,
		d.date,
		d.t_time,
		d.m_ingredient,
		d.t_weather,
		d.hit,
		d.editdate,
		db.rcp_seq,
		db.rcp_nm,
		db.rcp_parts_dtls,
		db.rcp_pat2,
		db.rcp_way2,
		db.hash_tag,
		db.info_na,
		db.info_wgt,
		db.info_pro,
		db.info_fat,
		db.info_car,
		db.info_eng,
		db.manual_img01,
		db.manual_img02,
		db.manual_img03,
		db.manual_img04,
		db.manual_img05,
		db.manual_img06,
		db.manual_img07,
		db.manual_img08,
		db.manual_img09,
		db.manual_img10,
		db.manual_img11,
		db.manual_img12,
		db.manual_img13,
		db.manual_img14,
		db.manual_img15,
		db.manual_img16,
		db.manual_img17,
		db.manual_img18,
		db.manual_img19,
		db.manual_img20,
		db.manual01,
		db.manual02,
		db.manual03,
		db.manual04,
		db.manual05,
		db.manual06,
		db.manual07,
		db.manual08,
		db.manual09,
		db.manual10,
		db.manual11,
		db.manual12,
		db.manual13,
		db.manual14,
		db.manual15,
		db.manual16,
		db.manual17,
		db.manual18,
		db.manual19,
		db.manual20,
		db.att_file_no_main,
		db.att_file_no_mk
		from dish d  
 		right join dishDB db
		on db.RCP_SEQ = d.RCP_SEQ
	</select>

	<select id="dishLike" parameterType="int" resultType="int">
		select count(*) from dish_like where dnum=#{dnum} and mnum=#{mnum}
	</select>
	
	<update id="edit" parameterType="map">
		UPDATE dish
		SET title=#{title}, ingredient=#{ingredient}, recipe=#{recipe},url=#{url},
		editdate=NOW(),t_time=#{t_time}, m_ingredient=#{m_ingredient}, cookery=#{cookery}, t_weather=#{t_weather}
		WHERE dnum = #{dnum}
	</update>
	
	<update id="upHit" parameterType="Integer">
		INSERT INTO DISH (hit,rcp_seq, writer) VALUES ('1','271','a')
        ON DUPLICATE KEY UPDATE hit = hit+1
	</update>
		
	<delete id="delete" parameterType="Integer">
		DELETE FROM dish WHERE dnum = #{dnum};
		DELETE FROM dish_picture WHERE dnum = #{dnum}
	</delete>
	
	<delete id="delPicture" parameterType="Integer">
		DELETE FROM dish_picture
		WHERE dnum = #{dnum}
	</delete>
	
	<insert id="goDishLike" parameterType="Integer">
		insert into dish_like
		set dnum=#{dnum}, mnum=#{mnum} 
	</insert>
	
	<delete id="goDishDislike" parameterType="Integer">
		DELETE FROM dish_like
		WHERE dnum=#{dnum} and mnum=#{mnum} 
		
	</delete>
	
	
	
</mapper>