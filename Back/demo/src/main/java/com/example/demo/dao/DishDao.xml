<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.DishDao">
	<insert id="add" parameterType="map" useGeneratedKeys="true"
		keyProperty="dnum">
		insert into dish 
		set writer=#{writer}, title=#{title}, ingredient=#{ingredient}, recipe=#{recipe},
		url=#{url}, date = NOW(), picture=#{picture},
		t_time=#{t_time}, m_ingredient=#{m_ingredient}, cookery=#{cookery}, t_weather=#{t_weather}
	</insert>

</mapper>